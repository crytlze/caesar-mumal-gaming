services:
  - type: web
    name: caesar-mumal-gaming
    env: node
    buildCommand: npm install
    startCommand: node src/server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 10000
